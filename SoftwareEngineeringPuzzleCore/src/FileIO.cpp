#include "FileIO.h"

namespace fileio
{
    std::string BuildSaveText(const std::vector<std::string>& lines)
    {
        // TODO PUZZLE 14:
        // Software engineering idea: file formats need predictable structure.
        // Combine all lines with a newline between them.
        // Do not add an extra newline after the final line.
        return "";
    }

    std::vector<std::string> SplitLines(const std::string& text)
    {
        // TODO PUZZLE 15:
        // Software engineering idea: loading data means parsing text back into useful pieces.
        // Split text into separate strings at every '\n'.
        return {};
    }
}
