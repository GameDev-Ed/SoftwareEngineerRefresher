#include "DataStructures.h"

namespace structures
{
    int FindLargest(const std::vector<int>& numbers)
    {
        // TODO PUZZLE 5:
        // Software engineering idea: arrays/lists store groups of data.
        // Loop through numbers and track the biggest value.
        // Edge case: if numbers is empty, return 0.
        return 0;
    }

    int CountWord(const std::vector<std::string>& words, const std::string& target)
    {
        // TODO PUZZLE 6:
        // Software engineering idea: loops are how systems inspect collections.
        // Count every word that exactly matches target.
        return 0;
    }

    bool HasItem(const std::unordered_map<std::string, int>& inventory, const std::string& itemName)
    {
        // TODO PUZZLE 7:
        // Software engineering idea: maps/dictionaries connect a key to a value.
        // Find itemName in inventory and return true only if quantity > 0.
        return false;
    }
}
